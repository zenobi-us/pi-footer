import { DEFAULT_TEMPLATE } from './defaults';

/*
 * Runtime extension config surface.
 * Consumers can replace `template` to control rendered footer rows.
 */
export const Config = {
  template: DEFAULT_TEMPLATE,
};
